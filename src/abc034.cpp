#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <complex>
#include <fstream>

using namespace std;

int main(void){
    int x, y; cin >> x >> y;
    cout << ((x>y) ? "Worse" : "Better" ) << endl;
    return 0;
}
