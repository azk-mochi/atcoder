#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <complex>
#include <fstream>
#include <cmath>

using namespace std;

int main(void){
    string a, b, c;
    cin >> a >> b >> c;
    //わからんのでパス
    cout << toupper(a[0]) << toupper(b[0]) << toupper(c[0]) << endl;
    return 0;
}
